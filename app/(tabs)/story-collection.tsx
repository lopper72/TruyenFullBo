import React from 'react';
import { View, Text } from 'react-native';

const StoryCollectionScreen = () => {
  return (
    <View>
      <Text>Story Collection Screen</Text>
    </View>
  );
};

export default StoryCollectionScreen; // Phải có export default
